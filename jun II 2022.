
7.
enum TrafficSignal {

    Snaga{
        @Override
        public void action(){
            System.out.println("W");
        }
    }, 
    Energija{
        @Override
        public void action(){
            System.out.println("J");
        }
    }, 


    public abstract void jedinicaMere();
}

9. compareTo
@Override public int compareTo(Klasa k)
    {
        if (this.broj > k.broj) {
 
            // if current object is greater,then return 1
            return 1;
        }
        else if (this.broj < k.broj) {
 
            // if current object is greater,then return -1
            return -1;
        }
        else {
 
            // if current object is equal to o,then return 0
            return 0;
        }
10.
constructor B
constructor A
constructor B
B_5
A_20
A_20
Struct_A -> 25, 20

11.
public double koren(params int[] brojevi)
{
double suma = 0;
foreach(int i in brojevi)
	suma += Math.Sqrt(i);

return suma;
}
